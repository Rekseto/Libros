const domain = "http://192.168.1.16:3000";

// book
export const booksEndpoint = `${domain}/books/`;

// loan
export const loanEndpoint = `${domain}/loan/`;
// auth

export const loginEndpoint = `${domain}/auth/login`;
export const logoutEndpoint = `${domain}/auth/logout`;
export const registerEndpoint = `${domain}/auth/register`;
export const verifyEndpoint = `${domain}/auth/verify`;

// user

export const userEndpoint = `${domain}/user/`;

// users

export const usersEndpoint = `${domain}/users/`;

// category

export const categoriesEndpoint = `${domain}/categories/`;
// publishers

export const publishersEndpoint = `${domain}/publishers/`;
